import type { TDetailCommonProps } from './types';

const DetailMobile = ({
  product,
  prodImages,
  thumbImages,
  selectedImage,
  isGalleryPopup,
  setIsGalleryPopup,
  handleImageClick,
}: TDetailCommonProps) => {
  return (
    <div id="container-mobile">
      <div>mobile</div>
    </div>
  );
};

export default DetailMobile;
